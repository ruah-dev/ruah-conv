{
	"name": "@ruah-dev/conv",
	"version": "0.1.0",
	"description": "Convert API specs (OpenAPI) into agent-ready tool surfaces — MCP servers, function-calling schemas.",
	"type": "module",
	"bin": {
		"ruah-conv": "dist/cli.js"
	},
	"ruah": {
		"namespace": "conv",
		"description": "Convert API specs to agent-ready tool surfaces"
	},
	"files": [
		"dist/",
		"postinstall.mjs",
		"LICENSE",
		"README.md"
	],
	"scripts": {
		"build": "tsc && chmod +x dist/cli.js",
		"dev": "tsc --watch",
		"postinstall": "node postinstall.mjs",
		"start": "node dist/cli.js",
		"pretest": "tsc -p tsconfig.test.json",
		"test": "node --test dist-test/test/*.test.js",
		"lint": "biome check .",
		"format": "biome check --write .",
		"typecheck": "tsc --noEmit",
		"clean": "rm -rf dist dist-test",
		"prepublishOnly": "npm run clean && npm run build && npm test"
	},
	"keywords": [
		"ai",
		"agents",
		"mcp",
		"openapi",
		"convert",
		"function-calling",
		"tool-surface",
		"claude",
		"ruah"
	],
	"author": "ruah-dev",
	"license": "MIT",
	"publishConfig": {
		"access": "public"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/ruah-dev/ruah-conv.git"
	},
	"bugs": {
		"url": "https://github.com/ruah-dev/ruah-conv/issues"
	},
	"homepage": "https://github.com/ruah-dev/ruah-conv#readme",
	"engines": {
		"node": ">=18.0.0"
	},
	"peerDependencies": {
		"@ruah-dev/cli": ">=1.0.0"
	},
	"dependencies": {
		"yaml": "^2.7.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.10",
		"@types/node": "^25.5.2",
		"typescript": "^6.0.2"
	}
}
